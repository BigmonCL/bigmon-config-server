# Variables de entorno para Config Server
# Copia este archivo como .env y configura los valores apropiados

# Configuración del repositorio Git
GIT_REPO_URI=git@github.com:BigmonCL/bigmon-config-repo.git
GIT_DEFAULT_BRANCH=main
GIT_SEARCH_PATHS=.

# Configuración SSH
GIT_IGNORE_LOCAL_SSH=false
GIT_CLONE_ON_START=false
GIT_TIMEOUT=5

# Clave privada SSH (multilínea)
# IMPORTANTE: Esta debe ser la nueva clave SSH regenerada, NO la que estaba en el código
GIT_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
TU_NUEVA_CLAVE_PRIVADA_AQUI
-----END OPENSSH PRIVATE KEY-----"

# Configuración del servidor
SERVER_PORT=8888

# Profile activo (dev, prod, local)
SPRING_PROFILES_ACTIVE=dev